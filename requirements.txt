# Runtime deps. Pinned to minimum versions known to work with the current code.
# For a fully reproducible install, pip-freeze your venv into requirements.lock
# after a successful pip install -r requirements.txt and commit it locally.
openai-whisper>=20250625
yt-dlp>=2026.3.17
librosa>=0.11.0,<0.12
numpy>=2.2.6,<3.0
playwright>=1.59.0,<2.0
curl_cffi>=0.15.0,<0.16
ollama>=0.6.2,<1.0
openai>=2.36.0,<3.0
